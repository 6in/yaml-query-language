---
layout: default
title: For Non Engineers
---

# 👤 非エンジニア向けガイド

PM、PO、BA、ビジネスアナリストなど、非エンジニアの方向けのYQLガイドです。

## YQLとは？（簡単に）

YQLは、**データベースへの問い合わせを、読みやすい形式で書く方法**です。

### 従来の方法（SQL）

```sql
SELECT c.name, SUM(o.amount) AS total
FROM customers c
INNER JOIN orders o ON c.id = o.customer_id
WHERE o.order_date >= '2024-01-01'
GROUP BY c.name
ORDER BY total DESC
LIMIT 10;
```

### YQLで書くと

```yaml
query:
  select:
    - 顧客名: c.name
    - 売上合計: "SUM(o.amount)"
  from: c: customers
  joins:
    - type: INNER
      alias: o
      table: orders
      on: "c.id = o.customer_id"
  where:
    - "o.order_date >= '2024-01-01'"
  order_by:
    - field: 売上合計
      direction: DESC
  limit: 10
```

**ポイント:**
- 日本語でラベルを付けられる
- 構造が階層的で見やすい
- 何をしているか分かりやすい

## 非エンジニアがYQLを使う場面

### 1. 要件定義の場面

**従来:**
```
「先月の売上トップ10の顧客を知りたい」
→ エンジニアに口頭で伝える
→ 認識齟齬が発生
→ 手戻り
```

**YQLを使うと:**
```yaml
# 要件: 先月の売上トップ10顧客
query:
  select:
    - 顧客名: c.name
    - 売上合計: "SUM(o.amount)"
  from: c: customers
  joins:
    - type: INNER
      alias: o
      table: orders
      on: "c.id = o.customer_id"
  where:
    - "o.order_date >= '2024-11-01'"
    - "o.order_date < '2024-12-01'"
  order_by:
    - field: 売上合計
      direction: DESC
  limit: 10
```

**メリット:**
- 要件が構造化される
- エンジニアとの認識齟齬が減る
- そのままSQLに変換できる

### 2. レビューの場面

エンジニアが書いたYQLをレビューする：

```yaml
query:
  select:
    - 顧客ID: c.customer_id
    - 顧客名: c.name
    - ステータス: c.status
  from: c: customers
  where:
    - "c.status = 'active'"
```

**確認ポイント:**
- `select` に必要なカラムが含まれているか？
- `where` の条件は正しいか？
- 抜け漏れはないか？

### 3. 仕様書として

YQLをそのまま仕様書として使う：

```yaml
# 機能: 顧客一覧取得
# 作成者: 山田太郎
# 作成日: 2024-12-01

query:
  select:
    - 顧客ID: c.customer_id
    - 顧客名: c.name
    - メール: c.email
    - 登録日: c.created_at
  from: c: customers
  where:
    - "c.status = 'active'"
  order_by:
    - field: c.created_at
      direction: DESC
  pagination:
    page: "#{page:1}"
    per_page: "#{per_page:20}"
```

**メリット:**
- コードと仕様書が一致
- 更新漏れがない
- エンジニアがそのまま実装できる

## 覚えておくと便利な用語

| 用語 | 意味 | 例 |
|------|------|-----|
| `select` | 取得するカラム | 顧客名、売上合計 |
| `from` | 取得元のテーブル | customers（顧客テーブル） |
| `joins` | テーブルの結合 | 顧客テーブルと注文テーブルを結合 |
| `where` | 絞り込み条件 | ステータスが「有効」 |
| `order_by` | 並び順 | 売上の多い順 |
| `limit` | 取得件数 | 上位10件 |
| `group_by` | グループ化 | 顧客ごとに集計 |

## よくある質問

### Q: YAMLって何？

**A:** データを構造化して書くための形式です。インデント（字下げ）で階層を表します。

```yaml
# これがYAML
query:
  select:           # 1段目のインデント
    - name: c.name  # 2段目のインデント
```

### Q: SQLを知らなくても使える？

**A:** 基本的な構造は理解しやすいですが、複雑な条件を書く場合はSQLの知識があると便利です。まずは簡単な例から始めてみてください。

### Q: 自分で書く必要がある？

**A:** 必ずしも書く必要はありません。以下の使い方ができます：

1. **読む**: エンジニアが書いたYQLをレビュー
2. **コメントする**: 「この条件を追加して」など
3. **書く**: 簡単な要件をYAMLで構造化

### Q: 間違えたらどうなる？

**A:** YQLは実行前にチェックされるので、間違いがあればエラーになります。本番データを壊す心配はありません。

## 始め方

### Step 1: 読んでみる

まずは既存のYQLを読んで、構造を理解してみましょう。

### Step 2: 真似してみる

既存のYQLをコピーして、条件を変えてみましょう。

### Step 3: 書いてみる

簡単な要件をYAMLで書いてみましょう。

```yaml
# 「有効な顧客の一覧を取得したい」
query:
  select:
    - 顧客名: c.name
  from: c: customers
  where:
    - "c.status = 'active'"
```

## サポート

分からないことがあれば、エンジニアに聞いてください。YQLは「エンジニアと非エンジニアの共通言語」を目指しています。

